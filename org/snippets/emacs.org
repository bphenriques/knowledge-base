#+TITLE: Emacs
#+SETUPFILE: ./_index.org
#+ROAM_TAGS: snippets
#+ROAM_ALIAS:

* TODO Personalize deft for other directories

For now I do not need.

#+BEGIN_SRC elisp
;;function to run deft in specified directory
(defun bjm-deft (dir)
  "Run deft in directory DIR"
  (setq deft-directory dir)
  (switch-to-buffer "*Deft*")
  (kill-this-buffer)
  (deft)
)
#+END_SRC
